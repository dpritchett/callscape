version: "2"

linters:
  default: standard
  exclusions:
    rules:
      - linters: [errcheck]
        source: "fmt\\.Fprint"
      - linters: [errcheck]
        source: "defer.*\\.Close\\(\\)"
      - linters: [errcheck]
        path: _test\.go
